<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/mi_refresh"
        android:icon="@drawable/ic_refresh"
        android:title="Tải lại"
        app:showAsAction="always" />
    <item
        android:id="@+id/mi_new_size"
        android:title="Chọn màn mới"
        app:showAsAction="never" />
    <item
        android:id="@+id/mi_custom"
        android:title="Tạo game mới riêng"
        app:showAsAction="never" />
    <item
        android:id="@+id/mi_download"
        android:title="Tải game mới riêng" />

</menu>